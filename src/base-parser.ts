export abstract class BaseParser {


    // createDocument(metadata, Classes[])    
    // createMetadata(language: string, generator: string, framework: string): Metadata {
    //     return new Metadata();        
    // }

    // createClass(name, kind, Examples[], Properties[], Methods[])

    // createExample(description, code)
}